# filter_bad_images.py

Removes failed simulation images (all-zero slices) from numpy arrays generated by `SLiMsims_to_numpy.py`.

## Why

When a SLiM simulation doesn't have enough segregating sites after downsampling and filtering, the helper script raises a `ValueError` and the subprocess fails. The corresponding slice in the memmap stays all zeros. These empty images need to be removed before CNN training.

## Location

```
/u/project/ngarud/Garud_lab/Brendan/Utils/filter_bad_images/filter_bad_images.py
```

### Single file

```bash
python "/u/project/ngarud/Garud_lab/Brendan/Utils/filter_bad_images/filter_bad_images.py" hard_sorted_color.npy hard_clean.npy
```

### Multiple files (input/output pairs)

```bash
python "/u/project/ngarud/Garud_lab/Brendan/Utils/filter_bad_images/filter_bad_images.py" \
    hard_sorted_color.npy hard_clean.npy \
    soft_sorted_color.npy soft_clean.npy \
    neutral_sorted_color.npy neutral_clean.npy
```

### Verbose mode (prints indices of bad images)

```bash
python "/u/project/ngarud/Garud_lab/Brendan/Utils/filter_bad_images/filter_bad_images.py" --verbose \
    hard_sorted_color.npy hard_clean.npy \
    soft_sorted_color.npy soft_clean.npy \
    neutral_sorted_color.npy neutral_clean.npy
```

## Example output

```
============================================================
Input:  hard_sorted_color.npy
Output: hard_clean.npy
  Bad image indices: [12, 45, 78, 134, 201]
  Original:  500 images
  Kept:      495 images
  Removed:   5 images (1.0%)
  Output shape: (495, 154, 200, 2), dtype: int8
```

## Note on class balancing

After filtering, each class (hard/soft/neutral) will likely have a different number of images. Truncate all classes to the smallest count before training so the CNN sees balanced classes.